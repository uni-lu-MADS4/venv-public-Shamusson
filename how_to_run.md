---
title: "how_to_run"
output: html_document
---

To install the packages, open the Terminal (not the Console) and run the following command:

```{{bash}}
pip install -r requirements.txt
```

This will install all packages noted down in requirements.txt, allowing for further use inside this project.